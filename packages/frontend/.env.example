# URL base da API (em dev, o proxy do Vite já redireciona /api para localhost:3001)
VITE_API_URL=/api

# Versionamento e aviso de atualização (preenchidos automaticamente no build de produção)
VITE_APP_VERSION=development
VITE_VERSION_UPDATE_ENABLED=true
VITE_VERSION_CHECK_INTERVAL_MS=90000
